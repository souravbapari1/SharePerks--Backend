import { MultiFileValidatePipe } from './multi-file-validate.pipe';

describe('MultiFileValidatePipe', () => {
  it('should be defined', () => {
    expect(new MultiFileValidatePipe()).toBeDefined();
  });
});
